# PLUGIN-GOVERNANCE.md

Before enabling a plugin:

```text
need
→ provenance
→ compatibility
→ capabilities/permissions
→ data classification
→ failure behavior
→ security review when material
→ probe
→ enable
```

Do not install every listed reference backend.

Plugin manifests are data and must be validated before use.
