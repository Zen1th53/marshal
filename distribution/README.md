# Distribution

This directory defines how the reusable pack can be copied, verified, upgraded,
and rolled back across many repositories.

`PACK-MANIFEST.json` is generated from the final pack and contains file digests.
