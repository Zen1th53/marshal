# T29.A10 — Cross-Review, Integration Gate and Epic Release Evidence

EPIC: T29 Dynamic Task DAG
ATOMIC: A10 Cross-Review, Integration Gate and Epic Release
STATUS: **BLOCKED at independent Gemini cross-review; Codex/source lane and repository verification are complete.**

## PATH_MAPPING

- canonical `internal/dag/types.go` → `internal/dag/types.go`
- canonical `internal/dag/engine.go` → `internal/dag/engine.go`
- canonical persistence → `internal/store/dag.go`, `internal/store/migrations.go`
- canonical runtime integration → `internal/app/runtime.go`
- operator contract → `docs/dynamic-dag.md`
- release tests → `internal/dag/release_a10_test.go`, `internal/store/dag_a10_release_test.go`

## Commits

- CODEX_COMMIT: `22236ac2efd29ace67bdb6a270a16d6da7cf2010`
- GEMINI_COMMIT: `BLOCKED_NOT_EXECUTED` — no Gemini runner/credential is available in this sandbox. The TERRA pair protocol forbids self-certification of independent verification, so no substitute SHA is fabricated.
- INTEGRATION_COMMIT: `59f79ec` (`merge(T29.A10): integrate dynamic DAG release`)
- WAVE0_COMBINED_HEAD_AT_VERIFICATION: `7eb1c1983a74187df276ead4ffca8a6fdf3245fe`

All commits created in this execution range were verified as authored and committed by `Zen1th53 <extreme29@proton.me>`.

## TDD evidence

- FOCUSED_RED_COMMAND: `go test -mod=vendor ./internal/dag -run '^TestT29A10OperatorDocsNameStableErrorsAndEvents$' -count=1`
  - observed RED before the documentation change because `DAG_CYCLE` was absent from the operator contract.
- FOCUSED_GREEN_COMMAND: `go test -mod=vendor ./internal/dag -run 'T29' -count=1 -timeout=90s`
  - exit `0`; package result `ok github.com/Zen1th53/marshal/internal/dag`.
- RELEASE_RESTART_COMMAND: `go test -mod=vendor ./internal/store -run '^TestT29A10ReleaseFlowSurvivesRestartAndKeepsStableReadiness$' -count=1 -timeout=20s -v`
  - exit `0`; restart/readiness/topological-order/exact-retry release flow passed.

## Security, failure and retry evidence

Executed command:

```text
go test -mod=vendor ./internal/dag -run 'TestT29A04MutationFailsClosedWithoutAuthority|TestT29A04CanonicalFreshnessFailurePreventsMutation|TestT29A05EventFailureAfterCommitReconcilesOnExactRetry|TestDAGErrorIsMachineReadableAndSecretSafe' -count=1 -v
```

Result: exit `0`; all four named tests passed. This covers missing authority, stale canonical freshness, post-commit event-delivery failure with exact retry reconciliation, and bounded secret-safe typed errors.

Executed command:

```text
go test -mod=vendor ./internal/store -run 'TestT29A03CancelledMutationHasNoSideEffectAndSafeError|TestT29A08ConcurrentOppositeEdgesHaveOneCanonicalWinner|TestT29A08ExactTransitionRetryAcrossReopenDoesNotAdvanceRevision|TestT29A10ReleaseFlowSurvivesRestartAndKeepsStableReadiness' -count=1 -v
```

Result: exit `0`; all four named tests passed. This covers cancellation cleanup/zero side effect, cross-store contention/cycle safety, reopen retry idempotency and restart recovery.

NO_LEAK_SCAN: exercised by the named `TestDAGErrorIsMachineReadableAndSecretSafe` plus T29 A04/A05 secret-marker tests in the full suite; full repository test exited `0`.

## Fuzz / property evidence

Executed:

```text
go test -mod=vendor ./internal/dag -run '^$' -fuzz '^FuzzT29A07CycleInsertionDetectedForArbitraryChainLength$' -fuzztime=3s -timeout=30s
```

Result: exit `0`; `70,355` executions, `16` new interesting inputs, no failure.

## Repository verification

- GO_TEST_ALL:
  - command: `go test -p=1 -mod=vendor ./... -count=1 -timeout=180s`
  - exit `0`; included `internal/store` in `20.164s`, `internal/worker` in `0.253s`, and `internal/worktree` in `1.065s`.
- GO_VET_ALL:
  - command: `go vet -mod=vendor ./...`
  - exit `0`.
- GO_TEST_RACE:
  - command: `go test -race -p=1 -mod=vendor ./... -count=1 -timeout=120s`
  - exit `0`; included `internal/dag` in `1.127s`, `internal/events` in `1.679s`, and `internal/store` in `95.953s`.
- GIT_DIFF_CHECK:
  - command: `git diff --check`
  - exit `0`.
- RELEASE_PACK:
  - command: `python3 tools/release_verify.py . distribution/PACK-MANIFEST.json`
  - result: `{ "status": "PASS", "errors": [] }`.

## Events / errors implemented

Stable errors documented: `DAG_CYCLE`, `DAG_NODE_NOT_FOUND`, `DAG_DUPLICATE_EDGE`, `DAG_INVALID_CONDITION`.

Canonical event vocabulary documented/verified: `dag.node.added`, `dag.edge.added`, `dag.node.ready`, `dag.node.blocked`, `dag.cycle.rejected`.

## Provider conformance

T29 core is provider-neutral and does not dispatch a model provider. The repository-wide full test/race suites include all four first-class adapter packages (`codex`, `claude`, `gemini`, `opencode`) and exited `0`. This is **not** treated as a substitute for the required independent Gemini security review.

## Known limitations / gate

- Independent Gemini lane/cross-review is unavailable in this sandbox and therefore remains `BLOCKED_NOT_EXECUTED`.
- Per `MASTER/18-CODEX-GEMINI-PAIR-PROTOCOL.md`, self-certification cannot replace that review. T29 is therefore not marked fully accepted/released yet even though its source lane and verification matrix are green.
- PUSH_OR_PR_RESULT: not attempted from the isolated sandbox; no push result is fabricated.
