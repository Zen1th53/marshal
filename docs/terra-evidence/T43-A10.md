# T43.A10 — Cross-Review, Integration Gate and Epic Release Evidence

EPIC: T43 Structured Event Stream
ATOMIC: A10 Cross-Review, Integration Gate and Epic Release
STATUS: **BLOCKED at independent Gemini cross-review; Codex/source lane and repository verification are complete.**

## PATH_MAPPING

- canonical `internal/events/types.go` → `internal/events/types.go`
- canonical `internal/events/engine.go` → `internal/events/engine.go`
- canonical durable stream → `internal/store/events.go`, `internal/store/migrations.go`
- canonical runtime integration → `internal/app/runtime.go`
- operator contract → `docs/structured-events.md`
- release tests → `internal/events/release_a10_test.go`, `internal/store/events_a10_release_test.go`

## Commits

- CODEX_COMMIT: `40b248d6162db61e6d4b604f3512b2e0f6753f38`
- GEMINI_COMMIT: `BLOCKED_NOT_EXECUTED` — no Gemini runner/credential is available in this sandbox. The TERRA pair protocol forbids self-certification of independent verification, so no substitute SHA is fabricated.
- INTEGRATION_COMMIT: `7eb1c1983a74187df276ead4ffca8a6fdf3245fe`
- WAVE0_COMBINED_HEAD_AT_VERIFICATION: `7eb1c1983a74187df276ead4ffca8a6fdf3245fe`

All commits created in this execution range were verified as authored and committed by `Zen1th53 <extreme29@proton.me>`.

## TDD evidence

- FOCUSED_RED_COMMAND: `go test -mod=vendor ./internal/events -run '^TestT43A10OperatorDocsNameStableErrorsAndEvents$' -count=1`
  - observed RED before the documentation change because `EVENT_TYPE_INVALID` was absent from the operator contract.
- FOCUSED_GREEN_COMMAND: `go test -mod=vendor ./internal/events -run 'T43' -count=1 -timeout=90s`
  - exit `0`; package result `ok github.com/Zen1th53/marshal/internal/events`.
- RELEASE_RESTART_COMMAND: `go test -mod=vendor ./internal/store -run '^TestT43A10ReleaseFlowResumesAfterRestartAndRetry$' -count=1 -timeout=20s -v`
  - exit `0`; durable resume after restart and exact idempotent retry passed.

## Security, failure and retry evidence

Executed command:

```text
go test -mod=vendor ./internal/events -run 'TestT43A03CancelledProducerStopsBeforeDurableStore|TestT43A04ProducerFailsClosedWithoutAuthority|TestT43A04DecisionBindingAndFreshnessFailClosed|TestT43A05AuditFailureAfterMainCommitReconcilesOnRetry|TestEventErrorIsMachineReadableAndSecretSafe' -count=1 -v
```

Result: exit `0`; all named tests and wrong-event/expired/canonical-stale subtests passed. This covers cancellation cleanup, missing authority, exact decision binding/freshness, post-commit audit reconciliation and secret-safe typed errors.

Executed command:

```text
go test -mod=vendor ./internal/store -run 'TestT43A02AppendRejectsConfiguredSecretLiteral|TestT43A07CancelledAppendHasZeroDurableSideEffect|TestT43A08ConcurrentExactRetryAcrossStoresConvergesToOneSequence|TestT43A10ReleaseFlowResumesAfterRestartAndRetry' -count=1 -v
```

Result: exit `0`; all four named tests passed. This covers configured-secret rejection/absence from durable state, cancelled append zero side effect, cross-store exact-retry convergence and restart pagination/retry.

NO_LEAK_SCAN: exercised by configured-secret persistence tests and T43 A01/A02/A05/A07 secret-marker tests in the full suite; full repository test exited `0`.

## Fuzz / property evidence

Executed:

```text
go test -mod=vendor ./internal/events -run '^$' -fuzz '^FuzzT43A07EventValidationNeverAcceptsUnsafeText$' -fuzztime=3s -timeout=30s
```

Result: exit `0`; `92,412` executions, `74` new interesting inputs, no failure.

## Repository verification

- GO_TEST_ALL:
  - command: `go test -p=1 -mod=vendor ./... -count=1 -timeout=180s`
  - exit `0`; included `internal/store` in `20.164s`, `internal/worker` in `0.253s`, and `internal/worktree` in `1.065s`.
- GO_VET_ALL:
  - command: `go vet -mod=vendor ./...`
  - exit `0`.
- GO_TEST_RACE:
  - command: `go test -race -p=1 -mod=vendor ./... -count=1 -timeout=120s`
  - exit `0`; included `internal/dag` in `1.127s`, `internal/events` in `1.679s`, and `internal/store` in `95.953s`.
- GIT_DIFF_CHECK:
  - command: `git diff --check`
  - exit `0`.
- RELEASE_PACK:
  - command: `python3 tools/release_verify.py . distribution/PACK-MANIFEST.json`
  - result: `{ "status": "PASS", "errors": [] }`.

## Events / errors implemented

Stable errors documented: `EVENT_TYPE_INVALID`, `EVENT_SECRET_FIELD`, `EVENT_STORE_FAILED`, `EVENT_SEQUENCE_CONFLICT`.

Canonical self-observability events documented/verified: `events.appended`, `events.subscriber.dropped`, `events.schema.rejected`.

## Provider conformance

T43 core is provider-neutral. The repository-wide full test/race suites include all four first-class adapter packages (`codex`, `claude`, `gemini`, `opencode`) and exited `0`. This is **not** treated as a substitute for the required independent Gemini security review.

## Known limitations / gate

- Independent Gemini lane/cross-review is unavailable in this sandbox and therefore remains `BLOCKED_NOT_EXECUTED`.
- Per `MASTER/18-CODEX-GEMINI-PAIR-PROTOCOL.md`, self-certification cannot replace that review. T43 is therefore not marked fully accepted/released yet even though its source lane and verification matrix are green.
- PUSH_OR_PR_RESULT: not attempted from the isolated sandbox; no push result is fabricated.
