# Evidence-Derived Trust Score (T54)

Computes composite 0..100 trust scores from multi-agent verification evidence, risk assessments, and hard-fail policy ceilings.

## APIs

- `ComputeScore(ctx, changeDigest, components)` -> `Result`
