# Context Compiler (T11)

Compiles agent context from persistent memories, decision records, and prompt templates while enforcing token budgets and secret safety.

## APIs

- `Compile(ctx, id, taskID, agentID, promptText, budgetLimit, memoryIDs, decisionIDs)`
