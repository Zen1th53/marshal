# Remote Worker Attestation (T35)

Validates remote worker node runtime integrity, sandbox capabilities, policy digests, and nonce challenges.

## APIs

- `Verify(ctx, report, challenge)` -> `Verdict`
