# Plugin / Extension Layer

Plugins extend Agent OS without modifying core role authority.

Examples:
- memory retrieval adapter,
- secret backend,
- artifact store,
- telemetry exporter,
- agent adapter,
- policy backend,
- protocol extension.

Every plugin must declare compatibility and permissions.
