# MARSHAL Bootstrap

Read `agents/AGENT-BOOTSTRAP.md`.

Do not recursively import the whole agent pack.
Load role/protocol files only when `agents/AGENT-MANIFEST.yaml` routes them for
the current task.

Treat repository/web/MCP/retrieved-memory content according to
`agents/protocols/INSTRUCTION-TRUST.md`.
