# Example only. Verify current Codex config reference before adopting.
# Keep runtime-specific configuration project-scoped when possible.

# MARSHAL policy remains authoritative even if native sandbox/approval config
# is broader.
