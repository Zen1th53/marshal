# Agent OS Bootstrap

Read `agents/AGENT-BOOTSTRAP.md` first.

Follow repository-local instructions and the Agent OS precedence model.

Load only the assigned role and task-relevant protocols via
`agents/AGENT-MANIFEST.yaml`.

Do not claim completion without fresh evidence for the current HEAD.
