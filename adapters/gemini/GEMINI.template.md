# Agent OS Bootstrap

Read `agents/AGENT-BOOTSTRAP.md` first.

Do not load all files under `agents/`.
Use `agents/AGENT-MANIFEST.yaml` to load only the role/protocol/memory context
required for the current task.

If this is resumed work, verify repository HEAD before trusting memory.
